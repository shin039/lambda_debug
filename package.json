{
  "name": "lambda_test",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "test": "docker-compose up -d",
    "show": "docker logs lambda",
    "rm"  : "docker rm lambda"
  }
}
